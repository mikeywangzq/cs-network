# 📧 SMTP 客户端 - 学习项目

<div align="center">

**一个使用 C++ 和底层 TCP Socket 实现的极简 SMTP 客户端**

[![Language](https://img.shields.io/badge/Language-C%2B%2B-blue.svg)](https://isocpp.org/)
[![Standard](https://img.shields.io/badge/C%2B%2B-11-blue.svg)](https://en.wikipedia.org/wiki/C%2B%2B11)
[![License](https://img.shields.io/badge/License-Educational-green.svg)](LICENSE)

</div>

---

## 📖 项目简介

这是一个**教育性质**的 SMTP（Simple Mail Transfer Protocol）客户端实现，使用 C++ 和 POSIX Socket API 从零开始构建。通过这个项目，你可以深入理解：

- 📡 TCP Socket 网络编程基础
- 🔤 SMTP 协议的工作原理
- 🔐 Base64 编码算法
- 💬 应用层协议的"一问一答"通信模式

> ⚠️ **重要提示**：本项目**不包含 SSL/TLS 加密**，仅用于学习 SMTP 协议原理。现代公共邮件服务器（Gmail、163.com 等）都要求使用加密连接（端口 465/587），此实现仅适用于本地测试服务器或特定的未加密 SMTP 服务器。

---

## ✨ 功能特性

- ✅ **底层实现**：使用 POSIX Socket API（`socket`, `connect`, `send`, `recv`）
- ✅ **DNS 解析**：使用 `gethostbyname` 解析 SMTP 服务器地址
- ✅ **完整流程**：实现 SMTP 协议的完整交互流程
- ✅ **可选认证**：支持 AUTH LOGIN（Base64 编码）
- ✅ **跨平台**：支持 Linux/Unix 和 Windows（Winsock）
- ✅ **详细注释**：包含详细的中文注释，解释每个步骤

---

## 📋 SMTP 协议流程

SMTP 是一个基于 TCP 的纯文本协议，采用"客户端发命令 → 服务器回响应"的模式：

```
客户端                          服务器
  |                               |
  |  1. TCP 连接（端口 25）        |
  |------------------------------>|
  |                               |
  |  2. 接收欢迎消息              |
  |<------------------------------|
  |       220 smtp.example.com    |
  |                               |
  |  3. HELO 握手                 |
  |------------------------------>|
  |       HELO world              |
  |<------------------------------|
  |       250 OK                  |
  |                               |
  | [可选] 4. AUTH LOGIN 认证      |
  |------------------------------>|
  |       AUTH LOGIN              |
  |<------------------------------|
  |       334 VXNlcm5hbWU6        |
  |------------------------------>|
  |       (Base64 用户名)         |
  |<------------------------------|
  |       334 UGFzc3dvcmQ6        |
  |------------------------------>|
  |       (Base64 密码)           |
  |<------------------------------|
  |       235 Authentication OK   |
  |                               |
  |  5. MAIL FROM 指定发件人       |
  |------------------------------>|
  |       MAIL FROM: <from@...>   |
  |<------------------------------|
  |       250 OK                  |
  |                               |
  |  6. RCPT TO 指定收件人         |
  |------------------------------>|
  |       RCPT TO: <to@...>       |
  |<------------------------------|
  |       250 OK                  |
  |                               |
  |  7. DATA 开始发送邮件内容      |
  |------------------------------>|
  |       DATA                    |
  |<------------------------------|
  |       354 Start mail input    |
  |                               |
  |  8. 发送邮件头和正文           |
  |------------------------------>|
  |       From: ...               |
  |       To: ...                 |
  |       Subject: ...            |
  |                               |
  |       邮件正文内容...          |
  |                               |
  |  9. 发送结束标志               |
  |------------------------------>|
  |       .                       |
  |<------------------------------|
  |       250 OK: queued          |
  |                               |
  | 10. QUIT 结束会话              |
  |------------------------------>|
  |       QUIT                    |
  |<------------------------------|
  |       221 Bye                 |
  |                               |
  |  关闭 TCP 连接                |
  |<----------------------------->|
```

### 🔑 关键点说明

1. **响应码**：SMTP 使用数字状态码
   - `220` - 服务就绪
   - `250` - 请求完成
   - `354` - 开始邮件输入
   - `334` - 认证数据请求
   - `235` - 认证成功
   - `221` - 服务关闭

2. **结束标志**：邮件内容必须以 `\r\n.\r\n` 结束（单独一行的点号）

3. **命令格式**：每个命令都以 `\r\n`（回车换行）结束

4. **邮件头与 SMTP 命令**：
   - SMTP 命令的 `MAIL FROM`/`RCPT TO` 是传输层的发件人/收件人
   - 邮件头的 `From:`/`To:` 是显示层的发件人/收件人（用户看到的）

---

## 🛠️ 编译与运行

### 前置要求

- **编译器**：支持 C++11 的编译器（GCC、Clang、MSVC）
- **操作系统**：Linux/Unix/macOS 或 Windows

### 方式一：使用 Makefile（推荐）

```bash
# 编译程序
make

# 运行程序
make run

# 清理编译文件
make clean

# 查看帮助
make help
```

### 方式二：手动编译

**Linux/Unix/macOS:**
```bash
g++ -Wall -std=c++11 -O2 -o smtp_client smtp_client.cpp
./smtp_client
```

**Windows (MinGW):**
```cmd
g++ -Wall -std=c++11 -O2 -o smtp_client.exe smtp_client.cpp -lws2_32
smtp_client.exe
```

**Windows (MSVC):**
```cmd
cl /EHsc /std:c++11 smtp_client.cpp ws2_32.lib
smtp_client.exe
```

---

## ⚙️ 配置说明

在运行程序前，需要修改 `smtp_client.cpp` 中的配置参数：

```cpp
// 在文件开头找到这些常量并修改
const string SMTP_SERVER = "smtp.example.com";  // SMTP 服务器地址
const int SMTP_PORT = 25;                        // SMTP 端口
const string FROM_EMAIL = "from@example.com";   // 发件人邮箱
const string TO_EMAIL = "to@example.com";       // 收件人邮箱
const string FROM_NAME = "发件人名称";           // 发件人显示名称
const string TO_NAME = "收件人名称";             // 收件人显示名称
```

### 如果需要认证（AUTH LOGIN）

取消注释并填写认证信息：

```cpp
const string USERNAME = "your_username";      // 邮箱用户名
const string PASSWORD = "your_password";      // 邮箱密码或应用专用密码
const bool NEED_AUTH = true;                  // 启用认证
```

然后在代码中找到 AUTH LOGIN 部分，取消注释相关代码块。

---

## 📁 项目结构

```
SMTP/
├── smtp_client.cpp    # SMTP 客户端源代码（包含详细注释）
├── Makefile          # 编译脚本
└── README.md         # 项目说明文档
```

---

## 🔍 代码结构说明

### 主要函数

| 函数名 | 功能说明 |
|--------|---------|
| `base64_encode()` | Base64 编码函数，用于 AUTH LOGIN 认证 |
| `send_data()` | 发送数据到服务器，并显示发送内容 |
| `recv_response()` | 接收服务器响应，并显示接收内容 |
| `check_response()` | 检查服务器响应码是否符合预期 |
| `main()` | 主函数，包含完整的 SMTP 协议流程 |

### 执行流程

```cpp
1. [网络初始化] 解析服务器地址（gethostbyname）
2. [建立连接] 创建 Socket 并连接服务器
3. [SMTP 握手] 接收 220 欢迎消息 → 发送 HELO
4. [可选认证] AUTH LOGIN → 发送用户名密码（Base64）
5. [指定发件人] MAIL FROM 命令
6. [指定收件人] RCPT TO 命令
7. [开始传输] DATA 命令
8. [邮件内容] 发送邮件头和正文
9. [结束传输] 发送 \r\n.\r\n
10. [结束会话] QUIT 命令
11. [关闭连接] 关闭 Socket
```

---

## ⚠️ 注意事项与限制

### 限制

- ❌ **不支持 SSL/TLS**：无法连接到需要加密的现代邮件服务器（如 Gmail、163.com 的 465/587 端口）
- ❌ **简化的认证**：仅实现了 AUTH LOGIN，未实现 AUTH PLAIN、AUTH CRAM-MD5 等其他认证方式
- ❌ **无错误重试**：网络错误时不会自动重试
- ❌ **单收件人**：仅支持单个收件人（虽然 SMTP 协议支持多个 RCPT TO）

### 适用场景

- ✅ **本地测试**：使用本地 SMTP 测试服务器（如 [MailHog](https://github.com/mailhog/MailHog)、[Papercut](https://github.com/ChangemakerStudios/Papercut-SMTP)）
- ✅ **内网邮件**：企业内部未加密的 SMTP 服务器
- ✅ **学习研究**：理解 SMTP 协议原理

### 安全建议

- 🔒 **不要在公网上使用未加密连接传输邮件**
- 🔒 **不要在代码中硬编码真实的用户名和密码**
- 🔒 **生产环境请使用支持 SSL/TLS 的库**（如 libcurl、OpenSSL）

---

## 🧪 测试方法

### 方式一：使用本地 SMTP 测试服务器

推荐使用 **MailHog**（一个用于测试的 SMTP 服务器）：

```bash
# 1. 安装 MailHog（Docker）
docker run -d -p 1025:1025 -p 8025:8025 mailhog/mailhog

# 2. 修改配置
const string SMTP_SERVER = "localhost";
const int SMTP_PORT = 1025;
const bool NEED_AUTH = false;

# 3. 运行客户端
./smtp_client

# 4. 在浏览器打开 http://localhost:8025 查看收到的邮件
```

### 方式二：使用真实邮件服务器（需要支持25端口）

部分邮件服务器可能支持25端口的未加密连接（越来越少）：

```cpp
// 163 邮箱示例（注意：可能需要开启 SMTP 服务并使用授权码）
const string SMTP_SERVER = "smtp.163.com";
const int SMTP_PORT = 25;  // 注意：163 可能已不支持未加密25端口
const string FROM_EMAIL = "your_account@163.com";
const string USERNAME = "your_account@163.com";
const string PASSWORD = "your_auth_code";  // 使用授权码，不是登录密码
const bool NEED_AUTH = true;
```

---

## 📚 学习资源

### SMTP 协议文档

- [RFC 5321 - SMTP 协议标准](https://tools.ietf.org/html/rfc5321)
- [RFC 2045-2049 - MIME（邮件内容格式）](https://tools.ietf.org/html/rfc2045)
- [RFC 4954 - SMTP 认证](https://tools.ietf.org/html/rfc4954)

### Socket 编程

- [Beej's Guide to Network Programming（中文版）](https://beej-zhcn.netdpi.net/)
- [POSIX Socket API 文档](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/sys_socket.h.html)

### Base64 编码

- [RFC 4648 - Base64 编码](https://tools.ietf.org/html/rfc4648)
- [Base64 编码原理](https://en.wikipedia.org/wiki/Base64)

---

## 🚀 进阶扩展

如果你想深入学习，可以尝试以下扩展：

1. **添加 SSL/TLS 支持**
   - 使用 OpenSSL 库实现 STARTTLS 或直接 SSL 连接
   - 支持连接到 Gmail、Outlook 等现代邮件服务

2. **实现更多认证方式**
   - AUTH PLAIN
   - AUTH CRAM-MD5
   - AUTH XOAUTH2（OAuth 2.0）

3. **支持附件**
   - 实现 MIME multipart/mixed 格式
   - Base64 编码附件内容

4. **支持 HTML 邮件**
   - 实现 multipart/alternative 格式
   - 同时发送纯文本和 HTML 版本

5. **错误处理增强**
   - 添加超时机制
   - 网络错误重试
   - 更详细的错误信息

6. **命令行参数**
   - 使用命令行参数而非硬编码配置
   - 读取配置文件

---

## 🤝 贡献

这是一个学习项目，欢迎提出改进建议！

---

## 📄 许可

本项目仅用于教育和学习目的。

---

## 💡 常见问题

**Q: 为什么连接 Gmail/163.com 失败？**

A: 现代公共邮件服务器都要求 SSL/TLS 加密连接（端口 465 或 587），本项目未实现加密功能。请使用本地测试服务器（如 MailHog）进行测试。

**Q: 什么是授权码？**

A: 许多邮件服务商（如 163.com、QQ 邮箱）要求使用"授权码"而非登录密码来进行 SMTP 认证。授权码需要在邮箱设置中单独生成。

**Q: 如何查看详细的协议交互过程？**

A: 运行程序后，所有发送和接收的内容都会显示在控制台，前缀为 `>>>` 和 `<<<`。

**Q: 能否发送给多个收件人？**

A: SMTP 协议支持多个 `RCPT TO` 命令。你可以修改代码，在步骤 8 后添加多次 `RCPT TO` 调用。

**Q: 编译时出现 "undefined reference to socket" 错误？**

A: Linux/Unix 系统通常不需要额外链接库。Windows 系统需要添加 `-lws2_32` 链接选项。

---

<div align="center">

**📧 Happy Learning!**

如果这个项目对你有帮助，欢迎 ⭐ Star！

</div>
